import 'package:assurance/constants/strings.dart';
import 'package:flutter/material.dart';

class WidgetNoData extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(Strings.sSinDatos),
    );
  }
}
